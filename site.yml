- hosts: node1
  gather_facts: False
  
  tasks:
  - name: Ensure python 3 is installed on all hosts
    raw: test -e /usr/bin/python || (apt -y update && apt install -y python-minimal)

- hosts: node1 
  tasks:
    - import_role:
        name: docker

- hosts: node1
  tasks:
    - import_role:
        name: rabbitmq
      tags: rabbitmq